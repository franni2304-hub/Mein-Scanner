<Window x:Class="OnePieceCardScanner.Views.OcrTemplateAnalyzerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OCR Template Analyzer"
        Width="1450"
        Height="900"
        MinWidth="1150"
        MinHeight="720"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="OCR Template Analyzer" FontSize="27" FontWeight="Bold"/>
                <TextBlock x:Name="TemplateFolderText" Text="-" Foreground="#666666" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Neu laden" Width="110" Height="38" Margin="6,0,0,0" Click="Reload_Click"/>
                <Button Content="Ordner öffnen" Width="130" Height="38" Margin="6,0,0,0" Click="OpenFolder_Click"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="210"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#B0B0B0" BorderThickness="1" CornerRadius="6" Padding="8">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Zeichenklassen" FontSize="18" FontWeight="Bold" Margin="4,2,4,8"/>
                    <ListBox x:Name="CharacterList" SelectionChanged="CharacterList_SelectionChanged"/>
                </DockPanel>
            </Border>

            <Border Grid.Column="2" BorderBrush="#B0B0B0" BorderThickness="1" CornerRadius="6" Padding="8">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" x:Name="TemplateHeadingText" Text="Keine Zeichenklasse ausgewählt" FontSize="18" FontWeight="Bold" Margin="4,2,4,8"/>
                    <ListBox x:Name="TemplateList"
                             BorderThickness="0"
                             Background="Transparent"
                             HorizontalContentAlignment="Stretch"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             SelectionChanged="TemplateList_SelectionChanged">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Width="88"
                                        Height="108"
                                        Margin="4"
                                        Padding="3"
                                        BorderBrush="{Binding BorderBrush}"
                                        BorderThickness="2"
                                        CornerRadius="5"
                                        Background="{Binding Background}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="{Binding CharacterText}"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Center"/>
                                        <Border Grid.Row="1" Margin="0,5" Background="Black" BorderBrush="#CCCCCC" BorderThickness="1">
                                            <Image Source="{Binding Image}" Stretch="Uniform"/>
                                        </Border>
                                        <TextBlock Grid.Row="2"
                                                   Text="{Binding QualityText}"
                                                   TextAlignment="Center"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <Border Grid.Column="4" BorderBrush="#B0B0B0" BorderThickness="1" CornerRadius="6" Padding="12">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Statistik" FontSize="20" FontWeight="Bold"/>
                        <TextBlock x:Name="ClassStatisticsText" Text="Keine Daten" Margin="0,8,0,14" TextWrapping="Wrap"/>
                        <Separator/>
                        <TextBlock Text="Ausgewähltes Template" FontSize="18" FontWeight="Bold" Margin="0,14,0,8"/>
                        <Border Height="210" Background="Black" BorderBrush="#CCCCCC" BorderThickness="1">
                            <Image x:Name="SelectedTemplateImage" Stretch="Uniform"/>
                        </Border>
                        <TextBlock x:Name="SelectedTemplateText" Text="Kein Template ausgewählt" Margin="0,8,0,14" TextWrapping="Wrap"/>
                        <TextBlock Text="Beste ähnliche Templates" FontSize="18" FontWeight="Bold" Margin="0,4,0,8"/>
                        <ListBox x:Name="SimilarityList" Height="220"/>
                        <Button Content="Ausgewähltes Template löschen" Height="40" Margin="0,14,0,0" Click="DeleteSelectedTemplate_Click"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <DockPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock x:Name="StatusText" DockPanel.Dock="Left" Text="Bereit" VerticalAlignment="Center"/>
            <TextBlock x:Name="GlobalStatisticsText" DockPanel.Dock="Right" Text="-" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </DockPanel>
    </Grid>
</Window>
